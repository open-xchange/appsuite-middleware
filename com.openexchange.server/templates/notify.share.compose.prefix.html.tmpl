<div>
  <div>
  ${link}
  </div>

  <#if password?has_content>
  <div>
  ${password}
  </div>
  </#if>

  <#if expiration?has_content>
  <div>
  ${expiration}
  </div>
  </#if>

  <div>
  ${files}
  <#list filenames as filename>
    ${filename}
  </#list>
  </div>
  
</div>

