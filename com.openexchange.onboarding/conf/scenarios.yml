# The YAML configuration file for on-boarding scenarios
#
# Notation/meaning is:
# <unique-scenario-identifier>:
#     enabled: <true|false>
#       Boolean. If set to 'false' the scenario will not be available, useful for testing/enabling the scenario later on
#
#     type: <plist|manual|link>
#       String. The type of the scenario. Currently known:
#       "plist" for generating a plist configuration file for iOS and OSX devices,
#       "manual" for a description for the user for manual set-up
#       "link" for a link/URL to either Apple App Store or Google Play Store
#
#     link: <link>
#       Object. The link and type routing to the app in either Apple App Store or Google Play Store. ONLY VALID IN CASE "type" IS SET TO "link"!
#         url: <url>
#         String: The URL of the link. For specifying a property that provides the actual link, please use special "property" scheme; e.g. "property://com.openexchange.onboarding.app.mylink"
#
#         type: <appstore|playstore>
#         String: Optional. Denotes to what store the URL points to (if it is not a common download link not pointing to a commercial store. Supported values are "appstore" and "playstore"
#
#     providers: <provider-identifiers>
#       Array of strings. A list of providers that are supposed to "contribute" to this scenario.
#
#     alternatives: <alternative-scenarios>
#       Array of strings. A list of alternative/fall-back scenarios; e.g. the scenario for manual setup
#
#     icon: <file-name>
#       String. The name of the icon image file located in '/opt/open-xchange/templates' directory or the base64-encoded binary content
#
#     displayName_t10e: <display-name>
#       String. The translatable display name; will be available in scenarios.pot file
#
#     description_t10e: <description>
#       String. The translatable description; will be available in scenarios.pot file
#


### Generic Apps
################################################################################

# Only an example
myappleappinstall:
    # Disabled...
    enabled: false
    type: link
    link:
        url: property://com.openexchange.onboarding.app.store.url
        type: appstore
    providers: [app]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    ## icon: <icon>
    # The translatable display name
    ## displayName_t10e: <the display name>
    # The translatable description
    ## description_t10e: <the description>

# Only another example
mygoogleappinstall:
    # Disabled...
    enabled: false
    type: link
    link:
        url: http://play.google.com/store/apps/details?id=mygoogleapp
        type: playstore
    providers: [app]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    ## icon: <icon>
    # The translatable display name
    ## displayName_t10e: <the display name>
    # The translatable description
    ## description_t10e: <the description>

# Yet another example
myplainappinstall:
    # Disabled...
    enabled: false
    type: link
    link:
        url: http://my.host.invalid/path/to/the/installer
    providers: [app]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    ## icon: <icon>
    # The translatable display name
    ## displayName_t10e: <the display name>
    # The translatable description
    ## description_t10e: <the description>


### OX Drive App
################################################################################

drivewindowsclientinstall:
    enabled: true
    type: link
    providers: [drivewindowsclient]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: drivewindowsclient.png
    # The translatable display name
    displayName_t10e: "OX Drive Windows Client"
    # The translatable description
    description_t10e: "To use Drive on your device, simply download OX Drive Windows Client"


### OX Mail App
################################################################################

mailappinstall:
    # Disbled by default
    enabled: false
    type: link
    providers: [mailapp]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: mailapp.png
    # The translatable display name
    displayName_t10e: "OX Mail App"
    # The translatable description
    description_t10e: "To use Mail on your device, simply download OX Mail App"


### OX Drive App
################################################################################

driveappinstall:
    # Disbled by default
    enabled: false
    type: link
    providers: [driveapp]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: driveapp.png
    # The translatable display name
    displayName_t10e: "OX Drive App"
    # The translatable description
    description_t10e: "To use Drive on your device, simply download OX Drive App"


### CalDAV & CardDAV Sync
################################################################################

davsync:
    enabled: true
    type: plist
    providers: [carddav, caldav]
    alternatives: [davmanual]
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: davsync.png
    # The translatable display name
    displayName_t10e: "CalDAV & CardDAV Sync"
    # The translatable description
    description_t10e: "To use Calendar & Contacts on your device, simply enable CalDAV & CardDAV Sync"

#davsync_mybrand:
#    enabled: true
#    type: plist
#    providers: [carddav, caldav]
#    alternatives: [davmanual]
#    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
#    icon: <base64>
#    # The translatable display name
#    displayName_t10e: "OX Sync"
#    # The translatable description
#    description_t10e: "Synchronize all your appointments and contacts"

davmanual:
    enabled: true
    type: manual
    providers: [carddav, caldav]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: davsync.png
    # The translatable display name
    displayName_t10e: "CalDAV & CardDAV server details"
    # The translatable description
    description_t10e: "To setup CalDAV & CardDAV account, please use the folling server details"


### Exchange ActiveSync
################################################################################

eassync:
    # Disbled by default
    enabled: false
    type: plist
    providers: [eas]
    alternatives: [easmanual]
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: eassync.png
    # The translatable display name
    displayName_t10e: "Exchange ActiveSync"
    # The translatable description
    description_t10e: "Synchronize your emails, contacts, calendar, and tasks"

easmanual:
    # Disbled by default
    enabled: false
    type: manual
    providers: [eas]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: eassync.png
    # The translatable display name
    displayName_t10e: "Exchange ActiveSync"
    # The translatable description
    description_t10e: "Synchronize your emails, contacts, calendar, and tasks"


### IMAP/SMTP
################################################################################

mailsync:
    enabled: true
    type: plist
    providers: [mail]
    alternatives: [mailmanual]
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: mailsync.png
    # The translatable display name
    displayName_t10e: "IMAP/SMTP"
    # The translatable description
    description_t10e: "Access & send your emails"

mailmanual:
    enabled: true
    type: manual
    providers: [mail]
    alternatives: []
    # The name of the image file located in '/opt/open-xchange/templates' directory or base64-encoded image binary
    icon: mailsync.png
    # The translatable display name
    displayName_t10e: "IMAP/SMTP"
    # The translatable description
    description_t10e: "Access & send your emails"

