#!/usr/bin/perl
#%# family=auto
#%# capabilities=autoconf

use strict;
use JMX::Jmx4Perl;
use JMX::Jmx4Perl::Request;   # Type constants are exported here


if ( $ARGV[0] and $ARGV[0] eq "autoconf")
{
    if (-e "/opt/open-xchange/bundles/com.openexchange.http.grizzly/com.openexchange.http.grizzly.jar") {
        print "yes\n";
        exit 0;
    } else {
        print "no\n";
        exit 0;
    }
}

if ( $ARGV[0] and $ARGV[0] eq "config") {
    print "graph_title Grizzly HttpServerFilter\n";
    print "graph_args --base 1000 -l 0\n";
    print "graph_category Open Xchange\n";
    print "graph_vlabel Number\n";
    
    print "current-suspended-request-count.label,current-suspended-request-count \n";
    print "current-suspended-request-count.draw LINE1\n";
    print "current-suspended-request-count.type DERIVE\n";
    print "current-suspended-request-count.min 0\n";

    print "requests-cancelled-count.label requests-cancelled-count\n";
    print "requests-cancelled-count.draw LINE1\n";
    print "requests-cancelled-count.type DERIVE\n";
    print "requests-cancelled-count.min 0\n";

    print "requests-completed-count.label requests-completed-count\n";
    print "requests-completed-count.draw LINE1\n";
    print "requests-completed-count.type DERIVE\n";
    print "requests-completed-count.min 0\n";

    print "requests-received-count.label requests-received-count\n";
    print "requests-received-count.draw LINE1\n";
    print "requests-received-count.type DERIVE\n";
    print "requests-received-count.min 0\n";
    
    print "requests-timed-out-count.label requests-timed-out-count\n";
    print "requests-timed-out-count.draw LINE1\n";
    print "requests-timed-out-count.type DERIVE\n";
    print "requests-timed-out-count.min 0\n";
    exit 0
}

### Settings and connection Testing

my $jmx = new JMX::Jmx4Perl(url => $ENV{oxJolokiaUrl},
user => $ENV{oxJolokiaUser},
password => $ENV{oxJolokiaPassword});

### test if a connection can be made
my $request = new JMX::Jmx4Perl::Request({type => AGENT_VERSION});
my $response = $jmx->request($request);
if ($response->is_error()){
	my $status = $response->status();
	if ($status == 404) {
		die "Link to servlet might be wrong";
	}
	elsif ($status == 401) {
		die "Credentials to login might be wrong";
	}
	else {
		die "Something went wrong ",$status;
	}
}

###

$request = new JMX::Jmx4Perl::Request(READ,
	"org.glassfish.grizzly:name=HttpServerFilter,pp=/gmbal-root/HttpServer[HttpServer]/NetworkListener[NetworkListener[http-listener]],type=HttpServerFilter",
	"current-suspended-request-count");
$response = $jmx->request($request);
if ($response->is_ok()){
	print "current-suspended-request-count.value ",$response->value(),"\n";
}

$request = new JMX::Jmx4Perl::Request(READ,
	"org.glassfish.grizzly:name=HttpServerFilter,pp=/gmbal-root/HttpServer[HttpServer]/NetworkListener[NetworkListener[http-listener]],type=HttpServerFilter",
	"requests-cancelled-count");
$response = $jmx->request($request);
if ($response->is_ok()){
	print "requests-cancelled-count.value ",$response->value(),"\n";
}

$request = new JMX::Jmx4Perl::Request(READ,
	"org.glassfish.grizzly:name=HttpServerFilter,pp=/gmbal-root/HttpServer[HttpServer]/NetworkListener[NetworkListener[http-listener]],type=HttpServerFilter",
	"requests-completed-count");
$response = $jmx->request($request);
if ($response->is_ok()){
	print "requests-completed-count.value ",$response->value(),"\n";
}

$request = new JMX::Jmx4Perl::Request(READ,
	"org.glassfish.grizzly:name=HttpServerFilter,pp=/gmbal-root/HttpServer[HttpServer]/NetworkListener[NetworkListener[http-listener]],type=HttpServerFilter",
	"requests-received-count");
$response = $jmx->request($request);
if ($response->is_ok()){
	print "requests-received-count.value ",$response->value(),"\n";
}

$request = new JMX::Jmx4Perl::Request(READ,
	"org.glassfish.grizzly:name=HttpServerFilter,pp=/gmbal-root/HttpServer[HttpServer]/NetworkListener[NetworkListener[http-listener]],type=HttpServerFilter",
	"requests-timed-out-count");
$response = $jmx->request($request);
if ($response->is_ok()){
	print "requests-timed-out-count.value ",$response->value(),"\n";
}
