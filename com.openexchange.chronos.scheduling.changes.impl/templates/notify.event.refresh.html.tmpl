<!DOCTYPE html>
<html>
    <head>
        <title></title>
        <meta charset="UTF-8">
        <style type="text/css">
            ${templating.include( "notify.event.styles.css.tmpl" )}
        </style>
    </head>
    <body>
        <div class="content">
            ${templating.include( "notify.event.intro.html.tmpl" )}
            <div class="calendar-action">${labels.refreshIntroduction}</div>
            ${templating.include( "notify.event.show.mini.html.tmpl" )}
        </div>
    </body>
</html>
