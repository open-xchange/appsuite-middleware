====[       ${event.summary!""}       ]====

${labels.timezoneInfo!""}

${labels.whenLabel} ${formatters.dateSpec} ${formatters.interval}
<#if event.location??>
${labels.whereLabel} ${event.location!""}
</#if>


<#if event.description??>
====================================
${event.description!""}
====================================
</#if>

== ${labels.attendeesLabel} ==

<#list attendees as attendee>
${attendeeHelper.attendeeLine(attendee)}
</#list>

<#if resources??>
== ${labels.resourcesLabel} ==

<#list resources as resource>
${attendeeHelper.attendeeLine(resource)}
</#list>
</#if>

== ${labels.detailsLabel} ==

${labels.showAsLabel} ${labels.showAs}
${labels.createdLabel} ${formatters.created} - ${labels.creator}

${labels.directLink!""}

${labels.attachmentNote!""}

${labels.justification!""}