# Provide a list of secrets to be used for encryption of sensitive user data, one secret per line
# In regular intervals add a secret to this list
# Always add to the end of the list
# Never delete a secret entry that may have been used to encrypt data, as that data can't be retrieved again 

"<password>"
"<user-id> + '-' +  <random> + '-' + <context-id>"
