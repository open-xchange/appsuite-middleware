<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"></meta>
  <meta charset="UTF-8"></meta>
  <style type="text/css">

    .content {
      white-space: normal;
      color: black;
      font-family: OpenSans, Arial, Helvetica, sans-serif;
      font-size: 12pt;
      cursor: default;
    }

    .person {
      color: #00A0CD;
    }

    .link {
      cursor: pointer;
      text-decoration: underline;
      color: #00A0CD;
      font-weight: bold;
      margin: 0 0 0 1em;
    }

    .message {
      max-width: 550px;
      margin: 0 0 2em 1em;
      -webkit-user-select: text;
      -moz-user-select: text;
      user-select: text;
      cursor: text;
    }

    .message-intro {
      color: #888;
      margin-bottom: 0.5em;
    }

    .credential-container {
      margin: 2em 0 0 0;
    }

    .credentials {
      margin: 0.5em 0 0 0;
      border: 0;
    }

    .credentials td {
      padding-left: 1em;
    }

    .unpw {
     -webkit-user-select: text;
     -moz-user-select: text;
     user-select: text;
     cursor: text;
     font-weight: bold;
    }

  </style>
</head>
<body>
 <#escape x as x?html>
  <div class="content">
   <#if message?has_content>
    <div class="message-intro">
      ${message_intro}:
    </div>
    <div class="message">
      ${message}
    </div>
   </#if>

   <div>
     <a href="${link}"><span class="link">${link_intro}</span></a>
   </div>
   
   <#if recipient_type == "anonymous">
     <#if password?has_content>
       <div class="credential-container">
         ${credentials_intro}:
         <table class="credentials">
           <tr>
             <td>${password_field}:</td>
             <td><span class="unpw">${password}</td>
           </tr>
         </table>
       </div>
     </#if>
   <#elseif recipient_type == "guest">
     <#if password?has_content>
       <div class="credential-container">
         ${credentials_intro}:
         <table class="credentials">
           <tr>
             <td>${username_field}:</td>
             <td><span class="unpw">${username}</td>
           </tr>
           <tr>
             <td>${password_field}:</td>
             <td><span class="unpw">${password}</td>
           </tr>
         </table>
      </div>
     <#else>
       <div class="credential-container">
         <p>${credentials_intro}</p>
         <p><a href="${reset_pw_link}"><span class="link">${reset_pw_link_intro}</span></a></p>
       </div>
     </#if>
   </#if>
  </div>
 </#escape>
</body>
</html>