<?xml version="1.0" encoding="UTF-8" ?>
<config>
    <abortOnConfigurationError>
        ${solr.abortOnConfigurationError:true}
    </abortOnConfigurationError>

    <luceneMatchVersion>LUCENE_36</luceneMatchVersion>

    <dataDir>${solr.core.dataDir}</dataDir>
    <lib dir="${libDir:/opt/open-xchange/solr/lib}" />

    <configIndex>
        <useCompoundFile>false</useCompoundFile>
        <ramBufferSizeMB>32</ramBufferSizeMB>
        <mergeFactor>10</mergeFactor>
        <maxFieldLength>10000</maxFieldLength>
        <unlockOnStartup>true</unlockOnStartup>
        <reopenReaders>true</reopenReaders>
        <writeLockTimeout>1000</writeLockTimeout>
        <commitLockTimeout>10000</commitLockTimeout>
        <lockType>simple</lockType>
        <deletionPolicy class="solr.SolrDeletionPolicy">
            <str name="keepOptimizedOnly">false</str>
            <str name="maxCommitsToKeep">1</str>
        </deletionPolicy>
    </configIndex>

    <updateHandler class="solr.DirectUpdateHandler2" />

    <query>
        <maxBooleanClauses>1024</maxBooleanClauses>
        <filterCache class="solr.FastLRUCache" size="512" initialSize="512" autowarmCount="0" />
        <queryResultCache class="solr.LRUCache" size="512" initialSize="512" autowarmCount="0" />
        <documentCache class="solr.LRUCache" size="512" initialSize="512" autowarmCount="0" />
        <enableLazyFieldLoading>true</enableLazyFieldLoading>
        <queryResultWindowSize>20</queryResultWindowSize>
        <queryResultMaxDocsCached>200</queryResultMaxDocsCached>
        <listener event="newSearcher" class="solr.QuerySenderListener">
            <arr name="queries"></arr>
        </listener>
        <useColdSearcher>false</useColdSearcher>
        <maxWarmingSearchers>5</maxWarmingSearchers>
    </query>

    <!-- For debugging purposes only --><!-- -->
    <!-- <requestDispatcher handleSelect="true"> -->
    <!-- <requestParsers enableRemoteStreaming="false" multipartUploadLimitInKB="2048000" /> -->
    <!-- <httpCaching never304="true" /> -->
    <!-- </requestDispatcher> -->
    <!-- <requestHandler name="standard" class="solr.SearchHandler" default="false"> -->
    <!-- <lst name="defaults"> -->
    <!-- <str name="echoParams">explicit</str> -->
    <!-- </lst> -->
    <!-- </requestHandler> -->

    <requestHandler name="simpleSearch" class="solr.SearchHandler" default="true">
        <lst name="invariants">
            <str name="defType">edismax</str>
            <str name="echoParams">explicit</str>
            <float name="tie">0.01</float>
            <str name="qf">
                content_gen^1.0 content_de^1.0 content_en^1.0 content_fr^1.0 content_it^1.0 subject_gen^5.0 subject_de^5.0 subject_en^5.0
                subject_fr^5.0 subject_it^5.0 from^10.0 to^10.0
                cc^1.0 bcc^1.0
            </str>
        </lst>
        <lst name="defaults">
            <str name="fl">*</str>
        </lst>

        <arr name="components">
            <!-- TODO: evt. eigene component zum Aufloesen der Sprachfelder -->
            <str>query</str>
        </arr>
    </requestHandler>

    <requestHandler name="allSearch" class="solr.SearchHandler">
        <lst name="defaults">
            <str name="fl">*</str> <!-- Envelope Felder -->
            <str name="df">full_name</str>
            <str name="q.op">OR</str>
        </lst>

        <arr name="components">
            <str>query</str>
        </arr>
    </requestHandler>

    <requestHandler name="getSearch" class="solr.SearchHandler">
        <arr name="components">
            <str>query</str>
        </arr>
    </requestHandler>

    <requestHandler name="customSearch" class="solr.SearchHandler">
        <lst name="defaults">
            <str name="fl">*</str>
            <str name="q.op">OR</str>
        </lst>

        <arr name="components">
            <str>query</str>
        </arr>
    </requestHandler>

    <requestHandler name="spotlight_from" class="solr.SearchHandler">
        <lst name="defaults">
            <str name="df">from</str>
            <str name="echoParams">explicit</str>
            <str name="fl">uuid,account,full_name,id,from,to,cc,bcc,subject_gen,subject_en,subject_de,subject_fr,subject_it</str>
        </lst>
    </requestHandler>

    <requestHandler name="spotlight_to" class="solr.SearchHandler">
        <lst name="defaults">
            <str name="df">to</str>
            <str name="echoParams">explicit</str>
            <str name="fl">uuid,account,full_name,id,from,to,cc,bcc,subject_gen,subject_en,subject_de,subject_fr,subject_it</str>
        </lst>
    </requestHandler>

    <requestHandler name="spotlight_subject" class="solr.SearchHandler">
        <lst name="invariants">
            <str name="defType">edismax</str>
            <str name="echoParams">explicit</str>
            <float name="tie">0.01</float>
            <str name="qf">
                subject_gen^1.0 subject_en^1.0 subject_de^1.0 subject_fr^1.0 subject_it^1.0
            </str>
            <!-- <str name="mm">75%</str> -->
        </lst>
        <lst name="defaults">
            <str name="fl">uuid,account,full_name,id,from,to,cc,bcc,subject_gen,subject_en,subject_de,subject_fr,subject_it</str>
        </lst>

        <arr name="components">
            <str>query</str>
            <str>debug</str>
        </arr>
    </requestHandler>

    <requestHandler name="mlt" class="solr.MoreLikeThisHandler">
        <lst name="defaults">
            <str name="df">uuid</str>
            <str name="mlt.mintf">1</str>
            <str name="mlt.fl">subject_gen,subject_en,subject_de,subject_fr,subject_it</str>
            <str name="fl">uuid,account,full_name,id,from,to,cc,bcc,subject_gen,subject_en,subject_de,subject_fr,subject_it</str>
        </lst>
    </requestHandler>

    <requestHandler name="/update" class="solr.XmlUpdateRequestHandler">
        <lst name="defaults">
            <str name="update.chain">langid</str>
        </lst>
    </requestHandler>
    <updateRequestProcessorChain name="langid">
        <processor class="org.apache.solr.update.processor.TikaLanguageIdentifierUpdateProcessorFactory">
            <bool name="langid">true</bool>
            <str name="langid.fl">subject,content</str>
            <str name="langid.whitelist">de,en,fr,it</str>
            <str name="langid.langField">locale</str>
            <bool name="langid.map">true</bool>
            <str name="langid.threshold">0.2</str>
            <str name="langid.fallback">gen</str>
        </processor>
        <processor class="solr.LogUpdateProcessorFactory" />
        <processor class="solr.RunUpdateProcessorFactory" />
    </updateRequestProcessorChain>

    <requestHandler name="/update/javabin" class="solr.BinaryUpdateRequestHandler" />

</config>