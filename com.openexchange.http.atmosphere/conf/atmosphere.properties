# Atmosphere.properties
#
# This file contains properties specific to the atmosphere service.
# As several different backends can be used with the atmosphere framework
# the config will be kept at the same bundle that is used to expose the service

# The alias to use when registering the AtmosphereServlet into the URL
# namespace. Default value: /push
com.openexchange.http.atmosphere.alias=/push/*