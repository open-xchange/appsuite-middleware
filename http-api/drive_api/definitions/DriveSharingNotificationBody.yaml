type: object
properties:
  $ref: ./RawDriveShareTargetData.yaml
  entities:
    type: array
    description: An array of entity IDs representing the recipients of the notification.
    items:
      type: integer
  notification:
    $ref: "#/definitions/DriveNotificationData"