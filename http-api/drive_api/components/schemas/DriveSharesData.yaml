type: object
properties:
  directories:
    type: array
    description: An array of metadata objects describing the shared folders.
    items:
      $ref: '#/components/schemas/DriveDirectoryMetadata'
  files:
    type: array
    description: An array of metadata objects describing the shared files.
    items:
      $ref: '#/components/schemas/DriveFileMetadata'
