type: object
properties:
  $ref: ../../../http_api/components/schemas/sharemanagement/RawShareLinkData.yaml
  checksum:
    type: string
    description: The current checksum of the share target (available since v7.8.1).
