type: object
properties:
  name:
    type: string
    description: The node-health-check's name
  status:
    type: string
    description: The node-health-check's status
    enum:
      - UP
      - DOWN
  data:
    type: object
    description: JSON object containing node-health-check's additional data
