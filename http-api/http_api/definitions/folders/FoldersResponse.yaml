type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    type: array
    description: Array of folders. Each folder is described as an array itself.
    items:
      type: array
      items:
        type: object
        description: |
          Array with elements that contain the information of a folder specified by the corresponding
          identifiers in the `columns` parameter. Therefore, the element types can be distinguished.