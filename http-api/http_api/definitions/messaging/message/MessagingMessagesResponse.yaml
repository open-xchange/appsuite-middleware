type: object
properties:
  $ref: ../../common/CommonResponseData.yaml
  data:
    type: array
    description: Array of messages. Each message is described as an array itself.
    items:
      type: array
      items:
        type: object
        description: |
          Array with elements that contain the information of a message specified by the corresponding
          column names in the `columns` parameter. The element types can be distinguished.