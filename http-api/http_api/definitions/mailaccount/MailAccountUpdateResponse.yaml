type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    $ref: "#/definitions/MailAccountData"
  warnings:
    type: array
    description: An array of error objects that occurred during the creation of the account.
    items:
      $ref: "#/definitions/CommonResponse"