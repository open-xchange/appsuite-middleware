type: object
properties:
  id:
    type: string
    description: ID of a newly created calendar account.
  provider:
    type: string
    description: The provider for the calendar account.
  configuration:
    type: object
    description: Provider-specific configuration data of the calendar account.
  timestamp:
    type: integer
    format: int64
    description: The last modification date.
