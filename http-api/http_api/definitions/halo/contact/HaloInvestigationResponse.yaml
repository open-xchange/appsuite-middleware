type: object
properties:
  $ref: ../../common/CommonResponseData.yaml
  data:
    type: array
    description: Array of halo data objects. Each object is described as an array itself.
    items:
      type: array
      items:
        type: object
        description: |
          Array with elements that contain the information of a data object specified by the corresponding
          identifiers in the `columns` parameter. The element types can be distinguished.