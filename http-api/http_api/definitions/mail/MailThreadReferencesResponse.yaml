type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    type: array
    description: An array of JSON objects each representing a conversation.
    items:
      $ref: "#/definitions/MailThreadReferenceData"