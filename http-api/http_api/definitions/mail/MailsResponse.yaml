type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    type: array
    description: Array of mails. Each mail is described as an array itself.
    items: 
    	type: array
    	description: An array of email fields.
    	items: 
    		type: string
    		description: A mail field.