type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    type: array
    description: An array of JSON objects each describing the folder ID and object ID of one imported mail.
    items:
      $ref: "#/definitions/MailDestinationData"