type: object
properties:
  old_password:
    type: string
    description: The user's current password or `null` if the password wasn't set before (especially for guest users).
  new_password:
    type: string
    description: The new password the user wants to set or `null` to remove the password (especially for guest users).