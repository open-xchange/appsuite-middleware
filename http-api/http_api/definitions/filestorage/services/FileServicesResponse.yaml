type: object
properties:
  $ref: ../../common/CommonResponseData.yaml
  data:
    type: array
    description: An array of JSON objects each describing one service.
    items:
      $ref: "#/definitions/FileServiceData"