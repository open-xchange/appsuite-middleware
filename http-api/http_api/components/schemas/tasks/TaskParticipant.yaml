type: object
properties:
  id:
    type: integer
    description: User ID.
  type:
    type: integer
    description: |
      Type of participant. <br>
      * 1 (user),<br>
      * 2 (user group),<br>
      * 3 (resource),<br>
      * 4 (resource group),<br>
      * 5 (external user)
    enum:
      - 1
      - 2
      - 3 
      - 4
      - 5
  mail:
    type: string
    description: Mail address of an external participant.
