type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    type: array
    description: Array of application specific passwords.
    items:
      $ref: '#/components/schemas/AppPassword'