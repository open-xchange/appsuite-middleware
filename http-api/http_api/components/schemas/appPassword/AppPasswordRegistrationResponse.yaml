type: object
properties:
  $ref: ../common/CommonResponseData.yaml
  data:
    type: object
    description: Registration response data
    properties:
      password:
        type: string
        description: Password to use for the application login
      login:
        type: string
        description: The username to use for login
      newLogin:
        type: boolean
        description: Login may not be same as used by the user to login to appsuite.


