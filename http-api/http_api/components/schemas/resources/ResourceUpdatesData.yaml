type: object
properties:
  new:
    type: array
    description: Array of new resource objects.
    items:
      $ref: '#/components/schemas/ResourceData'
  modified:
    type: array
    description: Array of modified resource objects.
    items:
      $ref: '#/components/schemas/ResourceData'
  deleted:
    type: array
    description: Array of deleted resource objects.
    items:
      $ref: '#/components/schemas/ResourceData'
