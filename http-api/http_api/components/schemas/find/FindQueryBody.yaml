type: object
properties:
  facets:
    type: array
    description: An array of selected facets that shall be applied for search.
    items:
      $ref: '#/components/schemas/FindActiveFacet'
  options:
    $ref: '#/components/schemas/FindOptionsData'
  start:
    type: integer
    description: 'The start of a pagination, if desired.'
  size:
    type: integer
    description: 'The page size of a pagination, if desired.'
