type: object
properties:
  data:
    type: array
    description: An array of active sessions and information about those sessions.
    items:
      $ref: '#/components/schemas/SessionManagementData'
