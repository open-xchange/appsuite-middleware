type: object
description: Information if mail is encrypted or signed.  Requires Guard
properties:
  encrypted:
    type: boolean
    description: True if the mail is currently encrypted
  signed:
    type: boolean
    description: True if the email has signatures
