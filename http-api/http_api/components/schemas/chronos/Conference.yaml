type: object
properties:
  id:
    type: integer
    format: int32
    description: The internal identifier of the conference.
  uri:
    type: string
    description: The URI to access the conference.
  label:
    type: string
    description: The label used to convey additional details on the use of the URI.
  features:
    type: array
    description: The features describing the key capabilities of the conference system.
    items:
      type: string
      description: A feature.
  extendedParameters:
    $ref: ExtendedPropertyParameter.yaml
