/chronos?action=delete:
  put:
    operationId: deleteEvent
    tags:
      - Chronos
    summary: Deletes events.
    consumes:
      - application/json
    parameters:
      - $ref: "#/parameters/gblQueryParamSession"
      - in: query
        name: timestamp
        type: integer
        format: int64
        description: Timestamp of the last update of the events.
        required: true
      - in: body
        name: body
        description: A JSON array of JSON objects with the id and optionally the recurrence position of the event.
        required: true
        schema:
          type: array
          items: 
            $ref: "#/definitions/EventId"
    responses:
      200:
        description: |
          An json object containing the deleted event if the deletion was successfull. In case of errors the responsible fields in the response are
          filled (see [Error handling](#error-handling)).
        schema:
          $ref: "#/definitions/ChronosCalendarResultResponse"