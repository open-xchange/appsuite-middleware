/snippet?action=all:
  get:
    operationId: getAllSnippets
    tags:
      - snippet
    summary: Gets all snippets (**available since v7.0.0/v6.22.0**).
    parameters:
      - $ref: "#/parameters/gblQueryParamSession"
      - in: query
        name: type
        type: string
        description: A list of comma-separated types to filter, e.g. "signature".
        required: false
    responses:
      200:
        description: |
          A JSON object containing an array with data for all snippets. In case of errors the responsible fields in the response are
          filled (see [Error handling](https://documentation.open-xchange.com/latest/middleware/http_api/1_introduction.html#error-handling)).
        schema:
          $ref: "#/definitions/SnippetsResponse"