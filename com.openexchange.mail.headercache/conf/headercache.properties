# The mail header cache properties

# Whether header cache bundle is enabled
com.openexchange.mail.headercache.enable=true

# Whether header cache bundle is applied to external accounts only
com.openexchange.mail.headercache.externalOnly=true

# The default refresh rate for a mail account's folder
com.openexchange.mail.headercache.defaultRefreshRate=60000
