  <#--
    Include this at the bottom of your body element to display the footer image and text.
    You also need to include the common style template in your header section. See
    example.tmpl in the docs directory for details.
  -->

  <#if footer_text?has_content || footer_image?has_content>
  <hr />
    <div id="signature">
      <p id="signature_text">
        <#if footer_image_content_type?has_content && footer_image?has_content>
          <img class="logo" id="signature_image" src="data:${footer_image_content_type};base64,${footer_image}">
        </#if>
        <#if footer_text?has_content>
          ${footer_text?html}
        </#if>
      </p>
    </div>
  </#if>