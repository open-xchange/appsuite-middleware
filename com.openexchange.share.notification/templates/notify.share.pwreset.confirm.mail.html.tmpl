<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"></meta>
  <meta charset="UTF-8"></meta>
  <style type="text/css">
    #pwrc_set_new_pwd a.btn {
      <#if button_color?has_content>
  	    color: ${button_color};
  	  <#else>
  	    color: #ffffff;
      </#if>
      
      <#if button_background_color?has_content>
  	    background-color: ${button_background_color};
  	  <#else>
  	    background-color: #3c73aa;
      </#if>
      
      <#if button_border_color?has_content>
  	    border-color: ${button_border_color};
  	  <#else>
  	    border-color: #356697;
      </#if>

      padding: 5px 10px;
      font-size: 12px;
      line-height: 1.5;
      border-radius: 3px;

      text-align: center;
      text-decoration: none;
      vertical-align: middle;
    }
    
	#signature img.logo {
	  display: inline;
	  float: right;
	}

  </style>
</head>
<body>
 <#escape x as x?html>
 
  <p id="pwrc_greeting">
    ${pwrc_greeting}
  </p>
  
  <p id="pwrc_requestreceived">
    ${pwrc_requestreceived} ${pwrc_account}
  </p>
  
  <p id="pwrc_set_new_pwd">
    ${pwrc_set_new_pwd}
    <a class="btn" href="${pw_reset_confirm_link}"><span>${pw_reset_confirm_link_label}</span></a></p>
  </p>
  
  <p id="pwrc_ignore">
    ${pwrc_ignore}
  </p>
  
  <p id="pwrc_automated_mail">
    ${pwrc_automated_mail}
  </p>
  
  <p id="pwrc_thanks">
    ${pwrc_thanks}
    ${pwrc_the_team}
  </p>  
  
  <#if footer_text?has_content || footer_image?has_content>
  <hr />
    <div id="signature">
      <p id="signature_text">
        <#if footer_image_content_type?has_content && footer_image?has_content>
          <img class="logo" id="signature_image" src="data:${footer_image_content_type};base64,${footer_image}">
        </#if>
        <#if footer_text?has_content>
          ${footer_text}
        </#if>
      </p>
    </div>
  </#if>

 </#escape>
</body>
</html>