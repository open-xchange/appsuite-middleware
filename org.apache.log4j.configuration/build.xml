<?xml version="1.0" encoding="UTF-8"?>
<project name="org.apache.log4j.configuration overrides" basedir=".">

    <description>
        Special build for the org.apache.log4j.configuration fragment. Conffile 
    </description>

	<import file="build-project.xml"/>

    <target name="testFor3rdPartyLib" description="We need a directory for this bundle to be able to link log4j.xml into configuration directory">
        <property name="3rdPartyLib" value="true"/>
    </target>

    <target name="postInstall">
        <symlink link="${destDir}/${bundlesDir}/${Bundle-SymbolicName}/log4j.xml" resource="${confInstallDir}/log4j.xml"/>
    </target>

</project>
