<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"></meta>
  <meta charset="UTF-8"></meta>
  <#include "notify.common.style.html.tmpl">
</head>
<body class="content">
  <#escape x as x?html>
    <p>${salutation}</p>
    <p>${appConnected}</p>
    <p>${revokeAccess}</p>
    <#-- view item_type button -->
    <p><a class="btn" href="${settingsURL}"><span>${gotoSettings}</span></a></p>
  </#escape>

  <#include "notify.common.footer.html.tmpl">
</body>
</html>
